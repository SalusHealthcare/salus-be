# Use postgres/example user/password credentials
version: '3.1'

services:

  testdb:
    image: postgres
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: example